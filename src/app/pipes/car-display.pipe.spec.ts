import { CarDisplayPipe } from './car-display.pipe';

describe('CarDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CarDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
