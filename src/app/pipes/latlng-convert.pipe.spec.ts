import { LatlngConvertPipe } from './latlng-convert.pipe';

describe('LatlngConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new LatlngConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
