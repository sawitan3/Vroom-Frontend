import { BookingDisplayPipe } from './booking-display.pipe';

describe('BookingDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
