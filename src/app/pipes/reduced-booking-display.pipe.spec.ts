import { ReducedBookingDisplayPipe } from './reduced-booking-display.pipe';

describe('ReducedBookingDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ReducedBookingDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
